# 📝 Шпаргалка - Learning Polish Bot

## 🚀 Быстрый старт (1 минута)

```bash
# 1. Получите токен от @BotFather в Telegram

# 2. Создайте .env файл
echo "TELEGRAM_BOT_TOKEN=ваш_токен" > .env

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Запустите бота
python3 main.py
```

## 📋 Команды бота

| Команда | Что делает |
|---------|-----------|
| `/start` | Начать работу |
| `/word` | Получить слово |
| `/progress` | Показать прогресс |
| `/help` | Справка |
| `/restart` | Сбросить прогресс |

## 🔘 Кнопки

- 📖 Получить слово
- 🔔 Уведомления ВКЛ/ВЫКЛ
- 📊 Мой прогресс
- ❓ Помощь

## ⏰ Важные моменты

- ✅ Слова отправляются в **9:00 утра** (Warsaw time)
- ✅ Всего **300 слов** в базе
- ✅ **Нет повторов** пока не закончатся все слова
- ✅ После 300 слов - **автоперезапуск**

## 📁 Структура файлов

```
new-project/
├── main.py              # Основной код бота
├── database.py          # База данных
├── words_database.json  # 300 слов
├── requirements.txt     # Зависимости
├── .env                 # Токен (создать!)
├── README.md           # Документация
├── QUICKSTART.md       # Быстрый старт
├── FEATURES.md         # Описание функций
└── check_words.py      # Проверка базы слов
```

## 🛠 Полезные команды

```bash
# Проверить базу слов
python3 check_words.py

# Посмотреть логи бота
tail -f bot.log

# Проверить БД SQLite
sqlite3 polish_bot.db "SELECT COUNT(*) FROM users;"

# Запуск в фоне
nohup python3 main.py > bot.log 2>&1 &

# Остановить бота в фоне
pkill -f "python3 main.py"
```

## 🔧 Настройка времени отправки

В файле `main.py` найдите:

```python
scheduler.add_job(
    send_daily_words,
    trigger=CronTrigger(hour=9, minute=0),  # ← Измените час
    ...
)
```

## 🐛 Решение проблем

| Проблема | Решение |
|----------|---------|
| Бот не отвечает | Проверьте токен в `.env` |
| "Token not found" | Создайте файл `.env` |
| Слова не приходят в 9:00 | Проверьте, что бот запущен |
| База не создаётся | Проверьте права доступа |

## 📊 Проверка работы

```bash
# 1. Запустите бота
python3 main.py

# 2. В другом терминале проверьте
python3 -c "from database import Database; db = Database(); print(f'Users: {len(db.get_all_users_with_notifications())}')"

# 3. Проверьте слова
python3 check_words.py
```

## 🌐 Production deployment

```bash
# Создайте systemd service
sudo nano /etc/systemd/system/polish-bot.service

# Содержимое:
[Unit]
Description=Polish Learning Bot
After=network.target

[Service]
Type=simple
User=youruser
WorkingDirectory=/path/to/new-project
ExecStart=/usr/bin/python3 main.py
Restart=always

[Install]
WantedBy=multi-user.target

# Запустите
sudo systemctl start polish-bot
sudo systemctl enable polish-bot
sudo systemctl status polish-bot
```

## 📝 Быстрые тесты

### Тест 1: Получение слова
```python
python3 -c "
from main import WORDS_DATABASE
print(WORDS_DATABASE[0])
"
```

### Тест 2: База данных
```python
python3 -c "
from database import Database
db = Database()
db.add_user(12345, 'test')
print('✅ DB works!')
"
```

### Тест 3: JSON валидность
```bash
python3 -c "import json; json.load(open('words_database.json')); print('✅ JSON OK')"
```

## 🎯 Первые шаги после запуска

1. ✅ Найдите бота в Telegram по username
2. ✅ Отправьте `/start`
3. ✅ Нажмите "📖 Получить слово"
4. ✅ Проверьте `/progress`
5. ✅ Настройте уведомления

## 💡 Советы

- 🔹 Используйте виртуальное окружение (venv)
- 🔹 Держите бота запущенным для ежедневных уведомлений
- 🔹 Делайте бэкап `polish_bot.db` периодически
- 🔹 Проверяйте логи при проблемах
- 🔹 Обновляйте зависимости регулярно

## 🆘 Контакты

- 📧 Создайте issue в репозитории
- 📚 Прочитайте README.md для деталей
- 🚀 См. QUICKSTART.md для установки
- ✨ См. FEATURES.md для описания функций

---

**Готово! Бот работает! 🎉**






