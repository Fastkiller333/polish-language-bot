# 🚀 Быстрый старт - Learning Polish Bot

## Шаг 1: Получите токен бота

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте команду `/newbot`
4. Введите имя бота (например: `My Polish Learning Bot`)
5. Введите username бота (должен заканчиваться на `bot`, например: `my_polish_learning_bot`)
6. Скопируйте полученный токен (выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Настройте бота

Создайте файл `.env` в корне проекта:

```bash
echo "TELEGRAM_BOT_TOKEN=ваш_токен_сюда" > .env
```

Или создайте вручную файл `.env` со следующим содержимым:

```
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

## Шаг 3: Установите зависимости

```bash
pip install -r requirements.txt
```

Или с использованием виртуального окружения (рекомендуется):

```bash
# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте его
# На macOS/Linux:
source venv/bin/activate
# На Windows:
# venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

## Шаг 4: Запустите бота

```bash
python3 main.py
```

Вы должны увидеть:

```
🇵🇱 Learning Polish Bot started successfully!
Daily words will be sent at 09:00 Warsaw time
```

## Шаг 5: Протестируйте бота

1. Откройте Telegram
2. Найдите вашего бота по username (который вы указали в BotFather)
3. Нажмите `/start`
4. Нажмите кнопку "📖 Получить слово"
5. Готово! 🎉

## 🎯 Основные команды

После запуска бота попробуйте:

- `/start` - Начать работу
- `/word` - Получить слово
- `/progress` - Посмотреть прогресс
- `/help` - Справка

## 📝 Настройка уведомлений

По умолчанию бот будет отправлять слова каждый день в **9:00 утра по польскому времени (Europe/Warsaw)**.

Пользователи могут:
- ✅ Включить уведомления (по умолчанию включены)
- ❌ Выключить уведомления через кнопку в боте

## 🔧 Изменение времени отправки

Если хотите изменить время отправки (например, на 8:00), отредактируйте в `main.py`:

```python
scheduler.add_job(
    send_daily_words,
    trigger=CronTrigger(hour=8, minute=0),  # Было: hour=9
    ...
)
```

## ❓ Частые проблемы

### Бот не отвечает

- Проверьте, что токен правильно скопирован в `.env`
- Убедитесь, что бот запущен (видите сообщение "Bot started successfully")
- Проверьте, что нет ошибок в консоли

### "TELEGRAM_BOT_TOKEN not found"

- Убедитесь, что файл `.env` существует в корне проекта
- Проверьте, что токен написан без пробелов: `TELEGRAM_BOT_TOKEN=токен`

### Слова не отправляются в 9:00

- Бот должен быть запущен постоянно
- Проверьте часовой пояс системы
- Убедитесь, что пользователь включил уведомления в боте

## 🚀 Запуск в фоне (для production)

### Используя nohup:

```bash
nohup python3 main.py > bot.log 2>&1 &
```

### Используя systemd (Linux):

Создайте файл `/etc/systemd/system/polish-bot.service`:

```ini
[Unit]
Description=Learning Polish Telegram Bot
After=network.target

[Service]
Type=simple
User=your_username
WorkingDirectory=/path/to/new-project
ExecStart=/usr/bin/python3 /path/to/new-project/main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:

```bash
sudo systemctl daemon-reload
sudo systemctl enable polish-bot
sudo systemctl start polish-bot
sudo systemctl status polish-bot
```

## 📊 Проверка базы данных

База данных создаётся автоматически в файле `polish_bot.db`.

Чтобы посмотреть содержимое:

```bash
sqlite3 polish_bot.db "SELECT * FROM users;"
sqlite3 polish_bot.db "SELECT COUNT(*) FROM user_word_history;"
```

---

**Готово! Теперь ваш бот работает и готов помогать в изучении польского! 🇵🇱**

