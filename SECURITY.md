# 🔒 Безопасность - Learning Polish Bot

## ⚠️ КРИТИЧЕСКИ ВАЖНО

Этот документ содержит важную информацию о безопасности бота.

## 🔑 Защита токена бота

### ✅ Правильный способ хранения токена:

1. **Создайте файл `.env` в корне проекта:**

```env
TELEGRAM_BOT_TOKEN=ваш_настоящий_токен_здесь
```

2. **Убедитесь, что `.env` в `.gitignore`:**
   - ✅ Файл `.env` уже добавлен в `.gitignore`
   - ✅ Никогда не удаляйте его оттуда!

3. **Используйте автоматические скрипты:**
   - Скрипты `setup.sh` и `install_global.sh` автоматически создают шаблон `.env`
   - После запуска скрипта откройте `.env` и вставьте свой токен

### ❌ НИКОГДА не делайте это:

- ❌ Не коммитьте файл `.env` в git
- ❌ Не публикуйте токен в коде, скриптах, или файлах проекта
- ❌ Не делитесь токеном в issues, pull requests, или форумах
- ❌ Не отправляйте токен в открытые чаты или каналы
- ❌ Не храните токен в незашифрованных документах

## 🚨 Что делать если токен скомпрометирован

Если ваш токен стал публичным (например, был закоммичен в git), **НЕМЕДЛЕННО** выполните следующие шаги:

### 1. Отзовите старый токен:

1. Откройте Telegram и напишите [@BotFather](https://t.me/BotFather)
2. Отправьте команду: `/mybots`
3. Выберите своего бота из списка
4. Нажмите `Bot Settings`
5. Выберите `Revoke Token`
6. Подтвердите отзыв токена

### 2. Создайте новый токен:

1. В настройках бота выберите `Generate New Token`
2. Скопируйте новый токен
3. Обновите файл `.env` с новым токеном:

```bash
# В файле .env замените старый токен на новый:
TELEGRAM_BOT_TOKEN=новый_токен_здесь
```

### 3. Очистите историю git (если токен был закоммичен):

**Вариант А: Создать новый репозиторий (самый простой способ)**

```bash
# Удалите связь со старым репозиторием
rm -rf .git

# Создайте новый репозиторий
git init
git add .
git commit -m "Initial commit (cleaned)"

# Создайте новый репозиторий на GitHub и подключите его
git remote add origin https://github.com/username/new-repo.git
git push -u origin main
```

**Вариант Б: Использовать git-filter-repo (для опытных пользователей)**

```bash
# Установите git-filter-repo
pip install git-filter-repo

# Удалите файлы с токеном из истории
git filter-repo --path setup.sh --invert-paths
git filter-repo --path install_global.sh --invert-paths

# Принудительно отправьте изменения
git push origin --force --all
```

### 4. Убедитесь что токен удален везде:

```bash
# Проверьте все файлы на наличие токена
grep -r "8234558544" .

# Или замените номер на ваш старый токен
grep -r "ваш_старый_токен" .
```

## 🛡️ Лучшие практики

### Перед каждым коммитом:

1. **Проверяйте что коммитите:**
```bash
git status
git diff
```

2. **Убедитесь что `.env` не в списке:**
```bash
git status | grep .env
# Должно быть пусто!
```

3. **Используйте git-secrets для автоматической проверки:**
```bash
# Установка (macOS)
brew install git-secrets

# Настройка для репозитория
git secrets --install
git secrets --register-aws
```

### Для серверов (PythonAnywhere, VPS и т.д.):

1. **Создайте `.env` вручную на сервере:**
```bash
cd ~/polish_bot
nano .env
# Вставьте токен и сохраните (Ctrl+X, Y, Enter)
```

2. **Установите правильные права доступа:**
```bash
chmod 600 .env
# Только владелец может читать/писать
```

3. **Никогда не скачивайте `.env` с сервера на локальную машину**

### Ротация токенов:

Для дополнительной безопасности регулярно обновляйте токен:

1. Раз в 3-6 месяцев генерируйте новый токен
2. Обновляйте `.env` на всех серверах
3. Отзывайте старый токен

## 📋 Шаблон .env файла

Создайте файл `.env` со следующим содержимым:

```env
# Telegram Bot Token
# Получить можно у @BotFather в Telegram
# Команды: /newbot (создать нового) или /mybots (управление существующими)

TELEGRAM_BOT_TOKEN=замените_на_ваш_токен
```

## 📞 Сообщить об уязвимости

Если вы обнаружили уязвимость в безопасности, пожалуйста:

- **НЕ** создавайте публичный issue
- Свяжитесь с разработчиком напрямую
- Дайте разумное время для исправления перед публикацией

## ℹ️ Дополнительная информация

- [Telegram Bot Security Best Practices](https://core.telegram.org/bots/faq#how-do-i-make-my-bot-more-secure)
- [OWASP Secrets Management](https://owasp.org/www-community/vulnerabilities/Use_of_hard-coded_password)
- [Git-secrets на GitHub](https://github.com/awslabs/git-secrets)

---

**Помните: Безопасность бота - это ваша ответственность! 🔒**

